[project]
name = "backend"
version = "0.1.0"
description = "Python backend API service"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
